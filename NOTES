- nodes after the 2nd do not join correctly on startup

  - membership init makes a gen_server call, which even if not related,
    is not legal

- lots of tests still broken

- missing tests for node joining

- seeming instability in get/put as nodes join
